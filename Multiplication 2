#include <stdio.h>

int main() {
    int n, i = 1;


    scanf("%d", &n);

    if (n <= 1 || n >= 9) {
        printf("Invalid Input\n");
    } else {
    
        do {
            printf("%d x %d = %d\n", n, i, n * i);
            i++;
        } while (i <= n);
    }

    return 0;
}
